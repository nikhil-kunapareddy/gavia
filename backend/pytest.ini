[pytest]
testpaths = tests
pythonpath = .
addopts = --strict-markers -ra

markers =
    slow: exercises the real 36MB ONNX model. Deselect with `-m "not slow"`.
    workflow: end-to-end journeys across detection, storage and the API.
